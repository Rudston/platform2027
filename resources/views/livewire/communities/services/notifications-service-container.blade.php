@php
    /** @var \App\Models\Circles\Circle $circle */
@endphp
{{-- Placeholder: real Notifications UI + data ops (via NotificationsService) come later. --}}
<div class="rounded-lg border border-dashed border-border-muted p-6 text-sm text-muted">
    <p class="font-medium text-main">Notifications</p>
    <p class="mt-1">NotificationsServiceContainer — circle #{{ $circle->id }}</p>
</div>
