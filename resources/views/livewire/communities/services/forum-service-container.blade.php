@php
    /** @var \App\Models\Circles\Circle $circle */
@endphp
{{-- Placeholder: real Forum UI + data ops (via ForumService) come later. --}}
<div class="rounded-lg border border-dashed border-border-muted p-6 text-sm text-muted">
    <p class="font-medium text-main">Forum</p>
    <p class="mt-1">ForumServiceContainer — circle #{{ $circle->id }}</p>
</div>
