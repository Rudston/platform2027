@php
    /** @var \App\Models\Circles\Circle $circle */
@endphp
{{-- Placeholder: real Events UI + data ops (via EventsService) come later. --}}
<div class="rounded-lg border border-dashed border-border-muted p-6 text-sm text-muted">
    <p class="font-medium text-main">Events</p>
    <p class="mt-1">EventsServiceContainer — circle #{{ $circle->id }}</p>
</div>
